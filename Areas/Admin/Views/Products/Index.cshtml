@model List<Product>
@{
    int count = 0;
}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Product</h4>
                        <p class="card-description">
                            <a class="btn btn-outline-success btn-sm" asp-controller="products" asp-action="create">
                                <i class="mdi mdi-chevron-double-right"> <strong>Create</strong></i>
                            </a>
                        </p>
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>
                                            No
                                        </th>
                                        <th>
                                            Name
                                        </th>
                                        <th>
                                            Image
                                        </th>
                                        <th>
                                            Category
                                        </th>
                                        <th>
                                            Settings
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        count++;
                                        <tr>
                                            <td class="py-1">
                                                @count
                                            </td>
                                            <td>
                                                @item.Name
                                            </td>
                                            <td>
                                               <img style="width:150px;height:150px; " src="~/assets/img/portfolio/@item.Image  " />
                                            </td>
                                            <td>
                                                @item.Category.Name
                                            </td>
                                            <td>
                                                <a class="btn btn-info" asp-controller="products" asp-action="update" asp-route-id="@item.Id">
                                                    <i class="mdi mdi-brush"></i>
                                                </a>
                                                <a class="btn btn-danger" asp-controller="products" asp-action="delete" asp-route-id="@item.Id">
                                                    <i class="mdi mdi-delete-sweep"></i>
                                                </a>
                                                <a class="btn btn-secondary" asp-controller="products" asp-action="detail" asp-route-id="@item.Id">
                                                    <i class="mdi mdi-book-open-page-variant"></i>
                                                </a>
                                            </td>

                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
